# Server Configuration
# ------------------
PORT=5800                                    # Server port number (default: 5800)

# MongoDB Configuration
# -------------------
MONGODB_URI=mongodb://localhost:27017/db     # mongoose connection URI
                                            # Format: mongodb://[username:password@]host[:port]/database
                                            # Example: mongodb+srv://user:pass@cluster.mongodb.net/portfolio

# Redis Configuration
# -----------------
REDIS_URL=localhost:6379                     # Redis server URL (without protocol)
                                            # Format: hostname:port[/db-number]
                                            # Example: redis.example.com:6379/0
                                            # Note: Protocol (redis://) will be added automatically

REDIS_PASSWORD=                              # Redis server password (if authentication is enabled)
                                            # Leave empty if no authentication is required
                                            # Example: your-redis-password

# JWT Configuration
# ---------------
JWT_SECRET=your-secret-key                   # Secret key for JWT token generation
                                            # Should be a long, random string
                                            # Example: use a secure generated key

# Email Configuration
# -----------------
EMAIL_SERVICE=gmail                          # Email service provider (gmail, outlook, etc.)
EMAIL_USER=your@email.com                    # Email account username/address
EMAIL_PASS=your-app-specific-password        # Email account password or app-specific password
                                            # For Gmail, use App Password if 2FA is enabled
EMAIL_FROM=your@email.com                    # Sender email address (usually same as EMAIL_USER)

# Cloudinary Configuration
# ----------------------
CLOUDINARY_CLOUD_NAME=your-cloud-name        # Your Cloudinary cloud name
                                            # Found in Cloudinary Dashboard
CLOUDINARY_API_KEY=your-api-key             # Cloudinary API Key
                                            # Found in Cloudinary Dashboard
CLOUDINARY_API_SECRET=your-api-secret       # Cloudinary API Secret
                                            # Found in Cloudinary Dashboard
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name  # Cloudinary URL (optional)
                                            # Format: cloudinary://api_key:api_secret@cloud_name
                                            # Note: Can be used instead of individual settings above

# Note: This is an example configuration file.
# 1. Copy this file to '.env' in the same directory
# 2. Replace the placeholder values with your actual configuration
# 3. Never commit the actual .env file to version control
# 4. Keep your production credentials secure and separate from development